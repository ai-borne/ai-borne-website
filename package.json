{
  "name": "ai-borne-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "pre-commit": "npm run build && npm test -- --run",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "husky": "^9.1.7",
    "typescript": "^5.2.2",
    "vite": "^8.2.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "marked": "^18.0.7"
  }
}
