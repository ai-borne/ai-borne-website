{
  "name": "ai-borne-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "pre-commit": "npm run build && npm test -- --run",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "husky": "^9.1.7",
    "typescript": "^5.2.2",
    "vite": "^5.0.8",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "marked": "^18.0.7"
  }
}
